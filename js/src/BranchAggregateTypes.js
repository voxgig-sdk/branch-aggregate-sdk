// Typed models for the BranchAggregate SDK (JSDoc typedefs).
//
// GENERATED from the API model: main.kit.entity.<e>.fields[] and per-op
// params (op.<name>.points[].args.params[]). Field/param types come from the
// canonical type sentinels via @voxgig/sdkgen canonToType (source of truth:
// @voxgig/apidef VALID_CANON). Annotations only — no runtime effect. Do not
// edit by hand.

/**
 * @typedef {Object} Analytics
 * @property {string} [aggregation]
 * @property {number} [code]
 * @property {string} data_source
 * @property {Array} dimensions
 * @property {boolean} [enable_install_calculation]
 * @property {string} end_date
 * @property {Object} [filters]
 * @property {string} [granularity]
 * @property {string} [id]
 * @property {string} [job_id]
 * @property {string} start_date
 * @property {string} [status_url]
 */

/**
 * @typedef {Object} AnalyticsLoadMatch
 * @property {string} id
 * @property {string} app_id
 * @property {string} format
 * @property {number} limit
 */

/**
 * @typedef {Object} AnalyticsCreateData
 * @property {string} format
 * @property {number} limit
 * @property {string} [aggregation]
 * @property {number} [code]
 * @property {string} data_source
 * @property {Array} dimensions
 * @property {boolean} [enable_install_calculation]
 * @property {string} end_date
 * @property {Object} [filters]
 * @property {string} [granularity]
 * @property {string} [id]
 * @property {string} [job_id]
 * @property {string} start_date
 * @property {string} [status_url]
 */

